/// What differs between the two apps on the screens they share. The passenger
/// app has its own copy of this file; the shared screens read it and never
/// ask which app they are in.
enum AppFlavor {
    static let taglineKey = "app.tagline.driver"
}
