/// What differs between the two apps on the screens they share. The driver
/// app has its own copy of this file with its own words; the shared screens
/// read it and never ask which app they are in.
enum AppFlavor {
    static let taglineKey = "app.tagline"
}
