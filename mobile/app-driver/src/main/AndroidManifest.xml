<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Without it every vibrate() throws and is swallowed: the ride-request
         cue and the road warning were silent on a vibrate-mode ringer. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Package visibility, required since Android 11 and enforced by
         targetSdk 35. Without this block resolveActivity() returns null for a
         tel: or smsto: intent even when a dialler exists, so the safety
         controls would look present and do nothing when pressed, which is the
         exact failure the whole feature is meant to prevent.

         ACTION_DIAL, never ACTION_CALL: the app opens the dialler with the
         number filled in and the person presses call. No CALL_PHONE
         permission, and no permission dialog at the worst possible moment. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
    </queries>

    <application
        android:name=".VelroDriverApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Velro"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:usesCleartextTraffic="${cleartext}">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Velro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager is initialised through Hilt (the Application
             implements Configuration.Provider), so androidx.startup's own
             initialiser must not also run.

             This said node="merge" before, which is what a release build
             caught: merge *keeps* the provider and folds attributes into it.
             Only node="remove" takes it out. The comment claimed removal the
             whole time; the manifest did the opposite, and two initialisers
             raced at every cold start. Invisible in debug, because lint only
             treats it as fatal when assembling a release. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />
        <service
            android:name=".duty.DriverDutyService"
            android:exported="false"
            android:foregroundServiceType="location|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-duty alerts: incoming ride requests and mountain-road warnings for a signed-in driver." />
        </service>
    </application>
</manifest>
